rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The web app does not access Firestore directly in the MVP.
    // All reads/writes go through authenticated Cloud Functions.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
